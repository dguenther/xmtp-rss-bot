# Your private key (keep this secure!)
WALLET_KEY=0x...
# Encryption key for the local database
ENCRYPTION_KEY=your_encryption_key_here
# XMTP environment (dev, production, or local)
XMTP_ENV=dev
# Check for new posts every X minutes
POST_INTERVAL=5
# Number of posts to fetch at a time
POST_LIMIT=10
# Directory to store user data
DATA_DIR=data